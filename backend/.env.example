# WorkSync AI Backend - Hackathon Demo Environment Variables
# 7-day implementation using PAT authentication

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration (SQLite for hackathon demo)
DB_PATH=./database/worksync-ai.db

# Azure DevOps Configuration (PAT-only approach)
# Get your PAT from: https://dev.azure.com/[your-org] → Profile → Personal Access Tokens
# Required scopes: Work Items (Read), Projects (Read)
ADO_PERSONAL_ACCESS_TOKEN=your-pat-token-here
ADO_DEFAULT_ORGANIZATION=your-ado-organization
ADO_API_VERSION=7.0

# Security Configuration
BCRYPT_ROUNDS=10
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging Configuration  
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:3000